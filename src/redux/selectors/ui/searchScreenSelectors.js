// selector
export const getSearchValueState = (state) => state.ui.searchScreen.searchValue;

export const getCurrentSortState = (state) => state.ui.searchScreen.currentSort;

export const getCurrentOrderState = (state) => state.ui.searchScreen.currentOrder;

